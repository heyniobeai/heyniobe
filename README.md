# Niobe — heyniobe.ai

Landing page for Niobe, a private AI that learns how you think, then works for you.

Static site, no build step. `index.html` is self-contained (inline CSS/JS, no external requests).
Deployed via Cloudflare Pages.
